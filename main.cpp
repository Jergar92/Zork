#include "World.h"

int main(){

	int num, numrand;
	char yn;
	string name;
	srand(time(NULL));
	Exit b;
	World a;
	for (int i = 0; i < 13; i++){
		printf("%s   ----    %s\n", a.rooms[i].name,b);
		
	}
	system("pause");
}
